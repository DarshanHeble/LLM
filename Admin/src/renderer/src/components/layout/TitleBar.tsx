import { Box } from '@mui/material'

function TitleBar(): JSX.Element {
  return (
    <Box
      sx={{
        minWidth: '100%',
        bgcolor: 'White',
        color: 'black'
        // position: 'relative'
      }}
    >
      title
    </Box>
  )
}

export default TitleBar
